apiVersion: 1

datasources:
  - name: API Incendios (JSON)
    uid: incendios-api
    type: yesoreyeram-infinity-datasource
    access: proxy
    isDefault: false
    editable: true
    jsonData:
      auth_method: "none"
