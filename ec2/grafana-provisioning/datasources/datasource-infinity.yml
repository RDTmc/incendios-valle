apiVersion: 1

datasources:
  - name: API Incendios (JSON)
    uid: incendios-api
    type: yesoreyeram-infinity-datasource
    access: proxy
    isDefault: false
    editable: true
    jsonData:
      global_query_string: "?_format=infinity"
      auth_method: "none"
