## Cambios incluidos

### Backend (main.py)
- verify_token usa Header(None) en vez de default posicional (fix 401)
- Endpoint publico /api/public/dashboard-stats para Grafana

### Frontend
- Login.tsx: removido import useNavigate no usado (fix TS6133)
- MapaFocos: AbortController elimina duplicados en StrictMode, fetch real a /api/focos-activos
- Reporte: pasa report_id real via navigate state a Confirmacion
- Confirmacion: muestra folio real de DynamoDB, no mock
- Historial + Reporte: boton Cerrar Sesion con logout() + redirect
- api.ts: fallback URL cambiado a https://api.keogh.lat/api
- .env.example: URL actualizada

### Infraestructura
- Nginx: headers WebSocket agregados a location /dashboard/
- docker-compose: volumen compartido /data_api:ro en grafana
- Grafana: datasource JSON API + dashboard provisionado con 3 paneles
